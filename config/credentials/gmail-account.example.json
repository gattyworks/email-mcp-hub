{
  "_comment": "This is a PLACEHOLDER showing where per-account Gmail token files live.",
  "_comment2": "Real token files (gmail-personal.creds.json, gmail-gattyworks.creds.json) are generated automatically when you run the auth step — see README. They are gitignored.",
  "_do_not_commit": "Real *.creds.json files contain OAuth refresh tokens."
}
